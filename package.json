{
  "name": "wise-content-factory",
  "version": "0.1.0",
  "private": true,
  "description": "Wise Content Factory (WCF) — an AI Content Operating System.",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "node --test packages/*/test/*.test.js apps/gateway/test/*.test.mjs apps/omniroute-server/test/*.test.mjs examples/*.test.js",
    "example": "node examples/run-example.mjs",
    "omniroute:serve": "node apps/omniroute-server/server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "license": "UNLICENSED"
}
